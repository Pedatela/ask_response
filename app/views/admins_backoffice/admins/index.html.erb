<div class="row p-3">
  <div class="col-lg-12">
    <h1 class="page-header">Listando Administradores</h1>
  </div>
</div>
<div class="row">
  <table id="datatablesSimple" class="dataTable-table">
    <thead>
      <tr>
        <th>
          <a href="#">Id</a>
        </th>
        <th >
          <a href="#">Email</a>
        </th>
        <th>
          <%= link_to new_admins_backoffice_admin_path,  class: "btn btn-success btn-sm" do%>
            <i class="fa fa-plus"></i>
          <%end%>
        </th>
      </tr>
    </thead>
    <tbody>
      <% @admins.each do |admin|  %>
        <tr>
          <td><%= admin.id %></td>
          <td><%= admin.email %></td>
          <td>
            <%= link_to edit_admins_backoffice_admin_path(admin), class: "btn btn-primary btn-sm rouded-5"  do%>
              <i class="fa fa-edit"></i>
            <%end%>

            <%= link_to admins_backoffice_admin_path(admin), method: :delete,  class: "btn btn-danger btn-sm rouded-5"  do%>
               <i class="fa fa-trash"></i>
            <%end%>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= paginate @admins %>
</div>